[package]
name = "rouse-ports"
version = "0.1.0"
edition = "2021"

[dependencies]
rouse-core = { path = "../rouse-core" }
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
